SET TERM ^ ;

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$RELATIONS WHERE RDB$RELATION_NAME = 'T_SICREDI_EDI_FILES'
  )) THEN
    EXECUTE STATEMENT '
      CREATE TABLE T_SICREDI_EDI_FILES (
        ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        FILE_NAME VARCHAR(255) NOT NULL,
        FILE_HASH VARCHAR(64) NOT NULL,
        EDI_KIND CHAR(1) NOT NULL,
        FILE_NUMBER VARCHAR(32),
        PROCESSING_DATE DATE,
        ACQUIRING_NAME VARCHAR(60),
        FILE_TYPE_DESCRIPTION VARCHAR(80),
        PROCESSING_TYPE VARCHAR(40),
        FILE_LAYOUT_VERSION VARCHAR(20),
        CLIENT_CODE VARCHAR(20),
        CLIENT_DOCUMENT VARCHAR(20),
        CLIENT_NAME VARCHAR(120),
        RAW_JSON BLOB SUB_TYPE TEXT,
        CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
      )';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'UX_SIC_EDI_FILES_HASH'
  )) THEN
    EXECUTE STATEMENT 'CREATE UNIQUE INDEX UX_SIC_EDI_FILES_HASH ON T_SICREDI_EDI_FILES (FILE_HASH)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$RELATIONS WHERE RDB$RELATION_NAME = 'T_SICREDI_EDI_SALES'
  )) THEN
    EXECUTE STATEMENT '
      CREATE TABLE T_SICREDI_EDI_SALES (
        ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        FILE_ID BIGINT NOT NULL,
        SECTION_KIND VARCHAR(16) NOT NULL,
        SUMMARY_RECORD_TYPE VARCHAR(3),
        DETAIL_RECORD_TYPE VARCHAR(3) NOT NULL,
        CLIENT_CODE VARCHAR(20),
        SALES_DATE DATE,
        SALE_DATETIME TIMESTAMP,
        CREDIT_DATE DATE,
        SALES_SUMMARY_NUMBER VARCHAR(32),
        SALES_RECEIPT_NUMBER VARCHAR(32),
        AUTHORIZATION_CODE VARCHAR(64),
        TERMINAL_NO VARCHAR(32),
        NSU_TRANSACTION VARCHAR(32),
        SYS_REF_NO VARCHAR(32),
        REF_FEPAS_NO VARCHAR(32),
        TRANSACTION_TYPE_CODE VARCHAR(8),
        TX_TYPE_DESC VARCHAR(80),
        TRANSACTION_STATUS_CODE VARCHAR(16),
        TX_STATUS_DESC VARCHAR(80),
        CARD_SCHEME_CODE VARCHAR(8),
        CARD_SCHEME_DESC VARCHAR(40),
        CARD_TYPE_CODE VARCHAR(8),
        CARD_TYPE_DESC VARCHAR(80),
        GROSS_AMOUNT NUMERIC(15,2),
        DISCOUNT_AMOUNT NUMERIC(15,2),
        NET_AMOUNT NUMERIC(15,2),
        FEE_PERCENT NUMERIC(9,4),
        INTERCHANGE_PLUS_AMOUNT NUMERIC(15,2),
        ACQUIRER_REFERENCE VARCHAR(64),
        PAY_ACCT_REF VARCHAR(64),
        MASKED_CARD_NUMBER VARCHAR(32),
        ID_UR VARCHAR(64),
        QR_CODE_STATUS VARCHAR(24),
        ROW_HASH VARCHAR(64) NOT NULL,
        RAW_JSON BLOB SUB_TYPE TEXT,
        CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
      )';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'UX_SIC_EDI_SALES_HASH'
  )) THEN
    EXECUTE STATEMENT 'CREATE UNIQUE INDEX UX_SIC_EDI_SALES_HASH ON T_SICREDI_EDI_SALES (ROW_HASH)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'IDX_SIC_EDI_SALES_FILE'
  )) THEN
    EXECUTE STATEMENT 'CREATE INDEX IDX_SIC_EDI_SALES_FILE ON T_SICREDI_EDI_SALES (FILE_ID)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'IDX_SIC_EDI_SALES_AUTH'
  )) THEN
    EXECUTE STATEMENT 'CREATE INDEX IDX_SIC_EDI_SALES_AUTH ON T_SICREDI_EDI_SALES (AUTHORIZATION_CODE, SALES_DATE)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'IDX_SIC_EDI_SALES_UR'
  )) THEN
    EXECUTE STATEMENT 'CREATE INDEX IDX_SIC_EDI_SALES_UR ON T_SICREDI_EDI_SALES (ID_UR)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$RELATIONS WHERE RDB$RELATION_NAME = 'T_SICREDI_EDI_FINANCE'
  )) THEN
    EXECUTE STATEMENT '
      CREATE TABLE T_SICREDI_EDI_FINANCE (
        ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        FILE_ID BIGINT NOT NULL,
        SECTION_KIND VARCHAR(16) NOT NULL,
        SUMMARY_RECORD_TYPE VARCHAR(3),
        DETAIL_RECORD_TYPE VARCHAR(3) NOT NULL,
        CLIENT_CODE VARCHAR(20),
        PAYMENT_DATE DATE,
        VALUE_DATE DATE,
        TRANSACTION_DATE DATE,
        TRANSACTION_TIME VARCHAR(8),
        PAY_INSTR_NO VARCHAR(32),
        FINANCE_SUMMARY_NUMBER VARCHAR(32),
        SALES_SUMMARY_NUMBER VARCHAR(32),
        SALES_REFERENCE VARCHAR(32),
        AUTHORIZATION_CODE VARCHAR(64),
        SALES_RECEIPT_NUMBER VARCHAR(32),
        PAYMENT_STATUS VARCHAR(32),
        PAYMENT_TYPE_CODE VARCHAR(8),
        PAYMENT_TYPE_DESCRIPTION VARCHAR(40),
        PAYMENT_TRANSACTION_TYPE_CODE VARCHAR(8),
        PAY_TX_TYPE_DESC VARCHAR(80),
        TRANSACTION_TYPE_CODE VARCHAR(8),
        TX_TYPE_DESC VARCHAR(80),
        CARD_SCHEME_CODE VARCHAR(8),
        CARD_SCHEME_DESC VARCHAR(40),
        RECEIPT_TYPE_CODE VARCHAR(8),
        RECEIPT_TYPE_DESC VARCHAR(32),
        GROSS_AMOUNT NUMERIC(15,2),
        DISCOUNT_AMOUNT NUMERIC(15,2),
        NET_AMOUNT NUMERIC(15,2),
        FEE_MODE_AMOUNT NUMERIC(15,2),
        FEE_MODE_NET NUMERIC(15,2),
        ACQUIRER_REFERENCE VARCHAR(64),
        PAY_ACCT_REF VARCHAR(64),
        MASKED_CARD_NUMBER VARCHAR(32),
        ID_UR VARCHAR(64),
        ROW_HASH VARCHAR(64) NOT NULL,
        RAW_JSON BLOB SUB_TYPE TEXT,
        CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
      )';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'UX_SIC_EDI_FIN_HASH'
  )) THEN
    EXECUTE STATEMENT 'CREATE UNIQUE INDEX UX_SIC_EDI_FIN_HASH ON T_SICREDI_EDI_FINANCE (ROW_HASH)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'IDX_SIC_EDI_FIN_FILE'
  )) THEN
    EXECUTE STATEMENT 'CREATE INDEX IDX_SIC_EDI_FIN_FILE ON T_SICREDI_EDI_FINANCE (FILE_ID)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'IDX_SIC_EDI_FIN_AUTH'
  )) THEN
    EXECUTE STATEMENT 'CREATE INDEX IDX_SIC_EDI_FIN_AUTH ON T_SICREDI_EDI_FINANCE (AUTHORIZATION_CODE, TRANSACTION_DATE)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'IDX_SIC_EDI_FIN_UR'
  )) THEN
    EXECUTE STATEMENT 'CREATE INDEX IDX_SIC_EDI_FIN_UR ON T_SICREDI_EDI_FINANCE (ID_UR)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$RELATIONS WHERE RDB$RELATION_NAME = 'T_SICREDI_EDI_RECEIVABLES'
  )) THEN
    EXECUTE STATEMENT '
      CREATE TABLE T_SICREDI_EDI_RECEIVABLES (
        ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        FILE_ID BIGINT NOT NULL,
        RECORD_TYPE VARCHAR(3) NOT NULL,
        UPDATE_DATE DATE,
        ID_UR VARCHAR(64) NOT NULL,
        RECEIVABLE_UNIT_KEY VARCHAR(64),
        RECEIVABLE_UNIT_STATUS VARCHAR(40),
        ACCREDITATION_DOCUMENT VARCHAR(20),
        CLIENT_DOCUMENT VARCHAR(20),
        CARD_SCHEME_CODE VARCHAR(8),
        CARD_SCHEME_DESCRIPTION VARCHAR(40),
        PAYMENT_DATE DATE,
        GROSS_AMOUNT NUMERIC(15,2),
        MDR_AMOUNT NUMERIC(15,2),
        UPDATED_TOTAL_AMOUNT NUMERIC(15,2),
        DISCOUNT_AMOUNT NUMERIC(15,2),
        ADVANCE_FEE_AMOUNT NUMERIC(15,2),
        PAID_AMOUNT NUMERIC(15,2),
        FREE_NEGOTIATION_AMOUNT NUMERIC(15,2),
        ALLOCATED_AMOUNT NUMERIC(15,2),
        ROW_HASH VARCHAR(64) NOT NULL,
        RAW_JSON BLOB SUB_TYPE TEXT,
        CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
      )';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'UX_SIC_EDI_REC_HASH'
  )) THEN
    EXECUTE STATEMENT 'CREATE UNIQUE INDEX UX_SIC_EDI_REC_HASH ON T_SICREDI_EDI_RECEIVABLES (ROW_HASH)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'IDX_SIC_EDI_REC_FILE'
  )) THEN
    EXECUTE STATEMENT 'CREATE INDEX IDX_SIC_EDI_REC_FILE ON T_SICREDI_EDI_RECEIVABLES (FILE_ID)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'IDX_SIC_EDI_REC_UR'
  )) THEN
    EXECUTE STATEMENT 'CREATE INDEX IDX_SIC_EDI_REC_UR ON T_SICREDI_EDI_RECEIVABLES (ID_UR)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$RELATIONS WHERE RDB$RELATION_NAME = 'T_SICREDI_EDI_RECEIVABLE_PAY'
  )) THEN
    EXECUTE STATEMENT '
      CREATE TABLE T_SICREDI_EDI_RECEIVABLE_PAY (
        ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        FILE_ID BIGINT NOT NULL,
        RECEIVABLE_ID BIGINT NOT NULL,
        RECORD_TYPE VARCHAR(3) NOT NULL,
        ID_UR VARCHAR(64) NOT NULL,
        RECEIVABLE_UNIT_KEY VARCHAR(64),
        CONTRACT_PRIORITY_ID VARCHAR(8),
        ACCOUNT_TYPE_CODE VARCHAR(8),
        ACCOUNT_TYPE_DESCRIPTION VARCHAR(40),
        ISPB VARCHAR(20),
        COMPE_CODE VARCHAR(8),
        AGENCY VARCHAR(16),
        PAYMENT_ACCOUNT VARCHAR(32),
        PAY_AMOUNT NUMERIC(15,2),
        BENEFICIARY_DOCUMENT VARCHAR(20),
        EFFECTIVE_SETTLEMENT_DATE DATE,
        EFFECTIVE_SETTLEMENT_AMOUNT NUMERIC(15,2),
        CONTRACT_IDENTIFIER VARCHAR(80),
        ROW_HASH VARCHAR(64) NOT NULL,
        RAW_JSON BLOB SUB_TYPE TEXT,
        CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
      )';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'UX_SIC_EDI_RECPAY_HASH'
  )) THEN
    EXECUTE STATEMENT 'CREATE UNIQUE INDEX UX_SIC_EDI_RECPAY_HASH ON T_SICREDI_EDI_RECEIVABLE_PAY (ROW_HASH)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'IDX_SIC_EDI_RECPAY_REC'
  )) THEN
    EXECUTE STATEMENT 'CREATE INDEX IDX_SIC_EDI_RECPAY_REC ON T_SICREDI_EDI_RECEIVABLE_PAY (RECEIVABLE_ID)';
END^

EXECUTE BLOCK AS
BEGIN
  IF (NOT EXISTS(
    SELECT 1 FROM RDB$INDICES WHERE RDB$INDEX_NAME = 'IDX_SIC_EDI_RECPAY_UR'
  )) THEN
    EXECUTE STATEMENT 'CREATE INDEX IDX_SIC_EDI_RECPAY_UR ON T_SICREDI_EDI_RECEIVABLE_PAY (ID_UR)';
END^

SET TERM ; ^
