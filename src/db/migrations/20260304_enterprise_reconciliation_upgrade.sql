ALTER TABLE T_RECONCILIATION ADD RECON_RULE_APPLIED VARCHAR(32);

CREATE INDEX IDX_CANON_MATCH ON T_INTERDATA_SALES (
  CANON_SALE_DATE,
  CANON_METHOD_GROUP,
  CANON_GROSS_AMOUNT
);

CREATE TABLE T_IMPORTED_FILES (
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  FILENAME VARCHAR(255) NOT NULL,
  HASH VARCHAR(64) NOT NULL,
  ACQUIRER VARCHAR(20) NOT NULL,
  IMPORTED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE UNIQUE INDEX UX_IMPORTED_FILES_HASH ON T_IMPORTED_FILES (HASH);
